#include "push_swap.h"


US ft_abs(short n)
{
	if (n < 0)
		return (-n);
	return (n);
}

short ft_max(short a, short b)
{
	if (a > b)
		return (a);
	return (b);
}

short ft_min(short a, short b)
{
	if (a < b)
		return (a);
	return (b);
}
